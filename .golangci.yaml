linters:
  enable-all: true
  disable:
    - tenv # Deprecated
    # Too noisy
    - exhaustruct
    - mnd
    - lll
    - varnamelen
    - funlen
    - cyclop
    - gocognit
linters-settings:
  depguard:
    rules:
      main:
        allow:
          - $gostd
          - github.com
